export const checkAndTriggerAlerts = async () => {
  // Placeholder to keep legacy alert cron operational.
  // Implement meaningful checks as soon as CSR alert rules are defined.
  return Promise.resolve();
};

export default {
  checkAndTriggerAlerts,
};
